# Ablation study — GPU 5, part 1: two of the three ARCHITECTURAL arms.
#   point_estimate_leaf  single posterior-blended head instead of the Bayes
#                        average over the regime family (leaf evaluation)
#   joint_selection      joint team-UCB child selection instead of the
#                        vectorized decoupled per-agent selection (search rule)
# (no_subjective is the third architectural arm — split into ablation_gpu5b.yaml
# on 2026-07-20 to run CONCURRENTLY with this half on the same card; two
# programs per GPU confirmed to coexist without a meaningful slowdown.)
#
# The conditioning-mechanism arms (moe_router / film) run on GPU 6 — they are a
# matched pairwise comparison and are kept on one card so they share hardware
# conditions. The unablated reference is the mazero_mixed row in
# compare_gpu3.yaml; do not duplicate it here.
#
# seed 0 only for now (quick full pass across every algo/arm/env first);
# seeds 1-2 are backfilled as a follow-up, not dropped.
algos:
  - mazero_mixed
ablations:
  - point_estimate_leaf
  - joint_selection
envs:
  - relation
seeds: [0]
total_env_steps: 200000
episodes: 16
num_pmcts: 1
