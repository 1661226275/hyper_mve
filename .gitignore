# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Training / eval artifacts (reproducible outputs)
runs/
checkpoints/
summary.json
*.log

# Local env
.venv/
.pytest_cache/

# Stray workflow scratch scripts (auto-persisted elsewhere)
.audit_*.js
*_workflow.js

# Vendored baseline clones (own .git; SHAs pinned in VENDOR.md)
hyper_mve/baselines/DIMA/
hyper_mve/baselines/HARL/
hyper_mve/baselines/MAZero/
hyper_mve/baselines/MBOM/
hyper_mve/baselines/m3w-marl/
hyper_mve/baselines/mamba/

# mazero_mixed fork: run outputs + ctree build artifacts (rebuild via make.sh)
hyper_mve/mazero_mixed/results/
hyper_mve/mazero_mixed/core/mcts/ctree/build/
hyper_mve/mazero_mixed/core/mcts/ctree/ctree_sampled/cytree.cpp
*.so
