# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Training / eval artifacts (reproducible outputs)
runs/
checkpoints/
summary.json
*.log

# Local env
.venv/
.pytest_cache/

# Stray workflow scratch scripts (auto-persisted elsewhere)
.audit_*.js
*_workflow.js

# Vendored baseline clones (own .git; SHAs pinned in comparison/vendor/VENDOR.md)
hyper_mve/comparison/vendor/*
!hyper_mve/comparison/vendor/VENDOR.md

# mazero_mixed fork: run outputs + ctree build artifacts (rebuild via make.sh)
hyper_mve/algo/mazero_mixed/results/
hyper_mve/algo/mazero_mixed/core/mcts/ctree/build/
hyper_mve/algo/mazero_mixed/core/mcts/ctree/ctree_sampled/cytree.cpp
*.so

# Run outputs under results/ (analysis CSVs stay tracked-optional)
results/**/tb/
results/**/harl_raw/
results/_logs/
# Checkpoints are large binaries (~5 MB each x ~72 grid runs). The small JSON
# artifacts next to them (eval_report/fidelity/meta/registry) stay trackable —
# those ARE the results.
results/**/ckpt.pt
