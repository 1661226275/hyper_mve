# 零样本 c 泛化 (Ch6.9, 必保实验) — Medium, 5 seeds.
# Rides cfg.eval.zero_shot_train_c / zero_shot_unseen_c → EvalReport
# return_zero_shot_seen / _unseen / _gap (no special override needed).
# 4 methods × 5 seeds = 20 runs.
meta:
  id: zero_shot_generalization
  title: "零样本 c 泛化 — seen vs unseen 保留率 (Medium, 5 seeds)"
  tier: must_have
  size: medium
  deliverables: ["Table 6.7", "Fig 6.5"]
  assertion: "B′ — 分布外 c 泛化优势 (per-context 生成 vs input 条件化)"
  runs_subdir: zero_shot
  blocked_on: []
  notes: "如需严格 train-c 限制 {0.2,0.5,0.8}, 后续加 env c-restriction override."
variants:
  - hyper
  - baseline_input_wide
  - baseline_input_deep
  - external_ma_muzero_gh
seeds: [0, 1, 2, 3, 4]
preset: medium
max_steps: 1000000
eval_planner_mode: planner_full
max_parallel: 2
n_gpus: 2
ablation_cell_id: zero_shot_generalization
